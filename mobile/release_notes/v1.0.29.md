# DiscountLink 1.0.29

- Improved tablet and large-screen layouts with better width limits for forms and app content.
- Added side navigation on data-heavy pages to reduce long scrolling.
- Reorganized the Flutter source into descriptive `core`, `features`, and `shared` folders.
- Split the previous single large `main.dart` implementation into focused class and widget files.
- Added coverage for the responsive login layout and updated Seller Hub tests.
